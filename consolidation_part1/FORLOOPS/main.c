#include <stdio.h>
#include <stdlib.h>

int main()
{
   int alpha,code;
   for (code = 1; code <= 7; code=code+1)
   {

   for (alpha = 'A'; alpha <= 'G'; alpha = alpha + 1)
   {

       {
           printf("%c\t", alpha);
       }
   }

   }

return(0);
}
